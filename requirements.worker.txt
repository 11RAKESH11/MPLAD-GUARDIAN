# MPLAD GUARDIAN — Worker Service Dependencies
# These packages are required by the standalone RQ worker container (Dockerfile.worker).

# Queue
rq>=1.16.0

# Redis client (rq depends on it but pinning ensures version compatibility)
redis>=5.0.0

# Database
psycopg2-binary>=2.9.9
SQLAlchemy>=2.0.0

# FastAPI/Pydantic used by shared backend modules imported in job functions
fastapi>=0.111.0
uvicorn>=0.30.1
pydantic>=2.7.1

# Utilities
python-dotenv>=1.0.0
